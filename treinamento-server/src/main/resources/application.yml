server:
    port: 8888

spring:
  application:
    name: treinamento-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/silvajosue/livraria-autorizacao #Alterar
          username: ${GITHUB_USER}
          password: ${GITHUB_PASS}
  