spring:
  application:
    name: treinamento-login
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"